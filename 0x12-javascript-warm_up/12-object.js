#!/usr/bin/node
// Script that searches the second biggest integer
// in the list of arguments.

const myObject = {
  type: 'object',
  value: 12
};

console.log(myObject);

myObject.value = 89;

console.log(myObject);
